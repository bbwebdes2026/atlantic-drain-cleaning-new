{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/build-gate.sh",
            "args": [],
            "if": "Bash(git push *)",
            "timeout": 300,
            "statusMessage": "Running build gate..."
          }
        ]
      }
    ]
  }
}
